# A release

Some prose about the release, with *emphasis*, **strong** and a
[link](https://example.com/release) in it, plus `a code span` for good
measure. The point of this file is to be ordinary: the speed test repeats
it until it is big, so it wants to hold the things a real document holds
rather than the corners a parser gets wrong.

## What changed

- A point about something that changed.
- Another point, with a [link](https://example.com/issue/1) in it.
- A third, longer point that runs on past the end of the line and keeps
  going onto a second one, the way a real note does.

## How to use it

```rust
fn main() -> usize {
    let total = items
        .iter()
        .copied()
        .sum::<usize>();
    return total;
}
```

> A quote about why any of this matters, which also runs on for long
> enough to be worth wrapping.

| Column | Meaning |
| ------ | ------- |
| one    | the first thing |
| two    | the second thing |

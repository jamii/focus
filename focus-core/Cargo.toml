[package]
name = "focus-core"
version = "0.0.0"
edition = "2024"

[dependencies]
bstr = "1"
similar = { version = "3", features = ["bytes"] }

[dev-dependencies]
syn = { version = "3", features = ["full"] }
prettyplease = "0.3"
